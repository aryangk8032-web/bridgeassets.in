import Link from "next/link";import {Footer,Header,PageHero} from "../components";import {properties} from "../inventory";export const metadata={title:"Upcoming projects"};export default function Projects(){const items=properties.filter(p=>p.category==="Project");return <main className="page-shell"><Header/><PageHero eyebrow="Projects / Upcoming" title="Early access to the addresses shaping tomorrow." text="Discover launch-stage and under-construction projects with clear progress context, micro-location insight and a realistic view of opportunity."/><section className="section"><div className="filter-bar"><a href="#">All projects</a><a href="#">Launching soon</a><a href="#">Under construction</a><a href="#">Gurugram</a></div><div className="project-list">{items.map(p=><article className="project-row" key={p.slug}><img src={p.image} alt={p.title}/><div><span className="badge">{p.status} · Early opportunity</span><h2>{p.title}</h2><p>{p.location} · {p.specs}</p><p>{p.description}</p><Link className="text-link" href={`/property/${p.slug}`}>Explore project details →</Link></div></article>)}</div></section><Footer/></main>}
