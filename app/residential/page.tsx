import {Footer,Header,ListingCard,PageHero} from "../components";import {properties} from "../inventory";export const metadata={title:"Residential properties"};export default function Residential(){const items=properties.filter(p=>p.category==="Residential");return <main className="page-shell"><Header/><PageHero eyebrow="Residential" title="Homes selected for how life is actually lived." text="Explore residences across Gurugram and Delhi NCR, thoughtfully shortlisted for quality, location and long-term value."/><section className="section"><div className="filter-bar"><a href="#">All homes</a><a href="#">Gurugram</a><a href="#">₹3 Cr – ₹6 Cr</a><a href="#">3 BHK</a><a href="#">Ready to move</a><a href="#">Verified only</a></div><div className="results-meta"><span>{items.length} curated residences</span><span>Sort: Recommended</span></div><div className="listing-grid">{items.map(p=><ListingCard key={p.slug} property={p}/>)}</div></section><Footer/></main>}
